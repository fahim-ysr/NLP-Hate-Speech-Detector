version: 2.1
orbs:
  aws-ecr: circleci/aws-ecr@8.2.1
  aws-cli: circleci/aws-cli@3.1.4
jobs:
  continuous-integration:
    docker:
      - image: cimg/base:stable
    resource_class: medium
    steps:
      - setup_remote_docker:
          docker_layer_caching: true

      - aws-ecr/build-and-push-image:
          create-repo: true
          dockerfile: Dockerfile
          path: .
          platform: linux/amd64
          push-image: true
          region: "${AWS_REGION}"
          repo: demo
          registry-id: AWS_ECR_REGISTRY_ID
          repo-scan-on-push: true
          tag: latest

  continuous-delivery:
    machine: true
    resource_class: fahim-ysr/nlp-hate-speech-detector-runner
    steps:
      - aws-cli/setup

      - run:
          name: auth to aws ecr
          command: aws ecr get-login-password --region ca-central-1 | docker login --username AWS --password-stdin 410371125954.dkr.ecr.ca-central-1.amazonaws.com

      - run:
          name: pull image from private repository
          command: docker pull 410371125954.dkr.ecr.ca-central-1.amazonaws.com/nlp-hate-speech-detector:latest

      - run:
          name: run image
          command: docker run -d -p 8080:8080 410371125954.dkr.ecr.ca-central-1.amazonaws.com/nlp-hate-speech-detector:latest
workflows:
  CICD:
    jobs:
      - continuous-integration
      - continuous-delivery:
          requires:
            - continuous-integration
